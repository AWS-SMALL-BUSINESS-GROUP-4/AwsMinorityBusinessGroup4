version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - amplify pull --appId d152usfsmg8ksh --envName dev --yes
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
